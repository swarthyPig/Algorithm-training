#include "stdafx.h"
#include <time.h>
#include <stdlib.h>
#include <Windows.h>

#pragma warning(disable:4996)
#define _CRT_SECURE_NO_WARNINGS

// ctrl + d 자동 복붙 // ctrl + k + d 전체 코드 정렬 // ctrl + k + f 블록부분만 정렬
// alt + 방향키위아래 : 이동
// 전체주석 : ctrl + k + c <---> ctrl + k + u

void func46() {
	// void 함수이름(전달 인수 없음);
	// void 함수이름(한개이상의 인수전달);
	// 한개의리턴 함수이름(전달 인수 없음);
	// 한개의리턴 함수이름(한 개 이상의 인수 전달);

	register int a = 10; //하드웨어로 만들어져 있는 변수
}

void main()
{
	srand((unsigned)time(NULL));

}